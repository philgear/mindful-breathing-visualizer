# Rust Breathing Visualizer

This directory contains a Rust CLI implementation of the breathing animation.

## Usage

1.  Make sure you have Rust installed (`cargo`).
2.  Run the application: `cargo run`
